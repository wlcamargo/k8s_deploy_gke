apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-color-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-color-service
            port:
              number: 80


